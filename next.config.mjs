/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/KeyBlaze-next.js",
  distDir: "dist",
  output: "export",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
